apiVersion: v1
kind: Service
metadata:
  name: {{ include "semgrep-k8s.fullname" . }}
  labels:
    {{- include "semgrep-k8s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "semgrep-k8s.selectorLabels" . | nindent 4 }}
